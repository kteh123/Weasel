name: Deploy Manual Github Page
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Manual Github Page
        run: |
          git subtree push --prefix Documents/Manual origin gh-pages